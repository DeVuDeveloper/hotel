<%= turbo_frame_tag user do %>
    <li class="bg-gray-800 text-gray-500 p-3 flex flex-col sm:flex-row gap-4 sm:justify-between border-b border-gray-700">
      <div class="w-full sm:w-1/3">
        <%= user.email %>
      </div>
      <div class="w-full sm:w-1/3 text-center">
        <% if user.admin? %>
          <div>Admin</div>
        <% elsif user.manager? %>
          <div>Manager</div>
        <% else %>
          <div>User</div>
        <% end %>
      </div>
      <div class="w-full sm:w-1/3 flex justify-end">
        <a href="#" class="text-gray-400 hover:text-gray-100 mr-2">
          <i class="material-icons-outlined text-base">visibility</i>
        </a>
        <%= link_to edit_admin_dashboard_user_path(user), class: "text-gray-400 hover:text-gray-100 mr-2" do %>
          <i class="material-icons-outlined text-base">edit</i>
        <% end %>
        <%= button_to admin_dashboard_user_path(user), method: :delete, class: "text-gray-400 hover:text-gray-100 mx-2" do %>
          <i class="material-icons-round text-base">delete_outline</i>
        <% end %>
      </div>
    </li>
  <% end %>


